{"title": "Tornado-01、ubuntu环境配置与连接pycharm - python web ", "index": "python,tornado,ubuntu", "content": "一、虚拟机的安装及配置\n\n首先在虚拟机里安装ubuntu这个是已经配置好了的ubuntu ova文件以及它的使用说明链接：https://pan.baidu.com/s/1GmnH... 密码：wjhd如果链接失效 请联系我更换 QQ:2696801532\n\n\n*注意：一定要看ova文件使用说明文档 切记虚拟机的帐号密码都在文档里有写mac地址要关闭虚拟机之后再更改*\n二、IDE与虚拟机的连接\n\nwindow下pycharm连接虚拟机的步骤\n1、关闭所有项目2、Create New Project3、pure python4、在本地新建一个文件夹tornado，并选中它5、点击下面的三角形按钮 展开interpret选项6、选中Existing interpret7、再点击右边的齿轮8、add remote-->选中SSH Credentials9、打开虚拟机-->查看虚拟机ip地址(ifconfig)\n\n\n\n10、Host填虚拟机ip，port默认就行，下面三行按要求填，最后的python interpreter path填写虚拟机中python的路径/home/pyvip/.virtualenvs/py3env/bin/python\n\n\n\n11、在ubuntu里新建一个文件夹tornado_1  (mkdir tornado_1）\n12、在pycharm中tools-->deployment-->configuration-->+-->随便写个名字（ubuntu_connect）--        >type选SFTP-->按要求填写配置 13、Root parh填刚刚新键的文件夹tornado路径-->选上左上角的对勾 下面的名字变粗体\n\n\n\n14、mappings设置在中间两行打上一个斜杠\n\n\n\n15、Test SFTP connection测试一下能否连接\n\n\n\n16、tools-->deployment-->Options-->选择Upload changed files automatically to the default server 这一项On explict save action\n\n\n\n17、tools-->start SSH session...-->再选择刚刚配置的ubuntu_connect\n18、在pycharm中 左上角tornado下新建一个start.py文件让后右键点击tornado文件夹 选择Upload to unbuntu_connect然后我们就可以在ubuntu中看到这个文件已经上传成功了\n\n\n\n*另外：vim .vimrc 命令打开vim配置文件 关于vim的配置信息都在这个文件内 注：不同的pycharm版本可能有些许的差异，但都大同小异虚拟机软件virtualbox和VMware以及VMware的激活密钥请找度娘 我就不贴出来了ubuntu等文件来源于潭州课堂*\n\n                ", "mainLikeNum": ["0 "], "mainBookmarkNum": "0"}